import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.imeiconnect.app',
  appName: 'IMEI Connect',
  webDir: 'out',
  android: {
    backgroundColor: '#f4f7f5',
  },
};

export default config;
